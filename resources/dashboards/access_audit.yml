resources:
  dashboards:
    access_audit:
      display_name: "Platform Access Audit"
      file_path: ../../dashboards/access_audit.lvdash.json
      warehouse_id: ${var.warehouse_id}
      embed_credentials: false
      permissions:
        - level: CAN_READ
          group_name: account users
