bundle:
  name: platform-governance

variables:
  warehouse_id:
    description: >
      SQL warehouse ID for governance statement execution.
      Injected at deploy time from terraform output team_warehouse_ids.data_platform_admins.

sync:
  exclude:
    - terraform/**
    - scripts/**
    - .github/**
    - "*.md"
    - .gitignore

targets:
  dev:
    default: true

include:
  - resources/jobs/*.yml
  - resources/dashboards/*.yml
